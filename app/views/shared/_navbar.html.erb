<nav class="navbar navbar-expand-lg navbar-light bg-light d-flex justify-content-between">
  <div class="navbar-left">
    <%= link_to 'Simple Airbnb', root_path, class: 'navbar-brand' %>
  </div>

  <div class="navbar-right d-flex">
    <%= form_tag root_path, method: :get, class: "search-bar form-inline" do %>
      <div class="input-area">
        <%= text_field_tag :query,
          params[:query],
          class: "form-control mr-1",
          placeholder: "Search for a flat"
        %>
        <%= submit_tag "Search", class: "btn btn-outline-success" %>
      </div>
    <% end %>

    <%= link_to 'Add Flat', new_flat_path, class: 'btn btn-outline-primary ml-2' %>
  </div>
</nav>
